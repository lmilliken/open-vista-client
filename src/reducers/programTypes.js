import { combineReducers } from 'redux';

const getProgramTypes=()=>{

    const programTypes=(state={}, action)=>{
switch(action.type){
    case 
}
    }

    return combineReducers({programTypes, isPending, errorMessage})
}
export default getProgramTypes